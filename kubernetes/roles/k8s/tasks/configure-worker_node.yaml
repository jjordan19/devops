- name: Add Firewalld Rule
  shell: firewall-cmd --permanent --add-port=10250/tcp --add-port=30000-32767/tcp
  when: hostvars[inventory_hostname].ansible_host in groups["nodes"]


- name: Reload Firewalld
  shell: firewall-cmd --reload
  when: hostvars[inventory_hostname].ansible_host in groups["nodes"]
 
- name: Check if kubelet.conf exists
  stat:
    path: "/etc/kubernetes/kubelet.conf"
  register: kubelet_conf
  when: hostvars[inventory_hostname].ansible_host in groups["nodes"]

- name: Cluster Join Command
  shell: kubeadm token create --print-join-command
  register: JoinCluster
  when: hostvars[inventory_hostname].ansible_host in groups["control_plane"]

- name: Join to cluster if needed
  shell: JoinCluster.stdout
  when: hostvars[inventory_hostname].ansible_host in groups["nodes"]

- name: Enable and check kubelet service
  systemd:
    name: kubelet
    daemon_reload: yes
    state: started
    enabled: yes
  when: hostvars[inventory_hostname].ansible_host in groups["nodes"]
